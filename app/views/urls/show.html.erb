<% if @shortened_full.present? %>
  <p>
    Your shortened URL:
    <a href="<%= @short_url.original_url %>" target="_blank" rel="noopener">
      <%= @shortened_full %>
    </a>
  </p>
<% else %>
  <h1>Welocome</h1>
  <p>This is the page of orignal url</p>
<% end %>

<p>
  <%= link_to "Shorten another", new_url_path %>
</p>
